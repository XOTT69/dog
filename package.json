{
  "name": "dog-coach-family-pwa",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0"
  },
  "scripts": {
    "test": "npm run test:syntax && npm run test:unit && npm run test:smoke",
    "test:syntax": "for f in $(find public api -name '*.js' -type f); do node --check \"$f\" >/dev/null || exit 1; done",
    "test:unit": "node scripts/test-utils-state.mjs",
    "test:smoke": "node scripts/smoke-static.mjs",
    "deploy:rules": "firebase deploy --only firestore:rules",
    "deploy:indexes": "firebase deploy --only firestore:indexes"
  }
}
